package controller;
/*
 Handles all interactions between the UI and the backend
 */
public class controller {
    //Method that is called by the UI WindowInterface upon submit being clicked.
    //generates the problems from ProblemGenerator
    //Feeds the problems (and filepath) into the ProblemsToFile side.

}
